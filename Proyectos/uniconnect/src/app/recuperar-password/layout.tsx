import type { Metadata } from "next";
import type { ReactNode } from "react";

export const metadata: Metadata = { title: "Recuperar contraseña" };

export default function RecuperarPasswordLayout({ children }: { children: ReactNode }) {
  return children;
}
